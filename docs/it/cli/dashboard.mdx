---
title: Visualizza sessioni
description: "Avvia la dashboard per sfogliare le sessioni degli agenti e gestire le policy"
---

```bash
failproofai
```

Avvia la dashboard web su `http://localhost:8020`.

## Opzioni

| Flag | Descrizione |
|------|-------------|
| `--port <number>` | Porta su cui mettersi in ascolto (default: `8020`) |
| `--projects-path <path>` | Sovrascrive il percorso in cui vengono cercate le cartelle dei progetti Claude Code |
| `--allowed-origins <origins>` | Host/IP separati da virgola autorizzati ad accedere alle risorse di sviluppo |

## Esempi

```bash
# Avvia su una porta diversa
failproofai --port 9000

# Usa un percorso personalizzato per i progetti
failproofai --projects-path ~/my-claude-projects
```