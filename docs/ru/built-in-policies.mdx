---
---
title: Встроенные политики
description: "39 встроенных политик, которые ловят распространённые режимы отказа агентов"
icon: shield
---

failproofai поставляется с 39 встроенными политиками, которые ловят распространённые режимы отказа агентов. Каждая политика срабатывает на определённом типе события хука и названии инструмента. Девятнадцать политик принимают параметры, позволяющие настроить их поведение без написания кода. Пять политик рабочего процесса обеспечивают выполнение конвейера commit → push → PR → CI перед тем, как Claude остановится.

---

## Обзор

Политики организованы по категориям:

| Категория | Политики | Тип хука |
|----------|----------|-----------|
| [Опасные команды](#опасные-команды) | block-sudo, block-rm-rf, block-curl-pipe-sh, block-failproofai-commands | PreToolUse |
| [Команды инфраструктуры](#команды-инфраструктуры) | block-kubectl, block-terraform, block-aws-cli, block-gcloud, block-az-cli, block-helm, block-gh-pipeline | PreToolUse |
| [Секреты (санитайзеры)](#секреты-санитайзеры) | sanitize-jwt, sanitize-api-keys, sanitize-connection-strings, sanitize-private-key-content, sanitize-bearer-tokens | PostToolUse |
| [Окружение](#окружение) | block-env-files, protect-env-vars | PreToolUse |
| [Доступ к файлам](#доступ-к-файлам) | block-read-outside-cwd, block-secrets-write | PreToolUse |
| [Git](#git) | block-push-master, block-work-on-main, block-force-push, warn-git-amend, warn-git-stash-drop, warn-all-files-staged | PreToolUse |
| [База данных](#база-данных) | warn-destructive-sql, warn-schema-alteration | PreToolUse |
| [Предупреждения](#предупреждения) | warn-large-file-write, warn-package-publish, warn-background-process, warn-global-package-install | PreToolUse |
| [Менеджеры пакетов](#менеджеры-пакетов) | prefer-package-manager | PreToolUse |
| [Рабочий процесс](#рабочий-процесс) | require-commit-before-stop, require-push-before-stop, require-pr-before-stop, require-no-conflicts-before-stop, require-ci-green-before-stop | Stop |

- **`block-`** — остановить агента и не дать ему продолжить.
- **`warn-`** — дать агенту дополнительный контекст, чтобы он мог исправить ситуацию.
- **`sanitize-`** — удалить конфиденциальные данные из выходных данных инструмента перед тем, как их увидит агент.

### Пространства имён

Каждая политика находится в слоте `<namespace>/<name>`. Встроенные политики принадлежат пространству имён **`exospherehost/`** — например, `exospherehost/sanitize-jwt`. Пространство имён предотвращает коллизии при загрузке пользовательских или сторонних политик с похожими короткими названиями.

В вашей конфигурации вы можете ссылаться на встроенную политику либо по её короткому названию, либо по полному названию; обе формы разрешаются в одну и ту же политику:

```json
{
  "enabledPolicies": [
    "sanitize-jwt",
    "exospherehost/block-rm-rf"
  ]
}
```

Если название не содержит `/`, failproofai считает его принадлежащим пространству имён по умолчанию `exospherehost`. Названия, уже содержащие `/` (например, `myorg/foo`, `custom/my-hook`), остаются без изменений.
- **`require-`** — заблокировать событие Stop до выполнения условий.

---

<Tip>
Каждая политика поддерживает необязательное поле `hint` в `policyParams`. Подсказка добавляется к сообщению deny или instruct, которое видит Claude, предоставляя практические рекомендации без изменения кода политики. Работает со встроенными, пользовательскими и условными политиками. Подробнее см. в разделе [Конфигурация → hint](/ru/configuration#hint-cross-cutting).
</Tip>

---

## Опасные команды

Предотвратить выполнение агентами операций, которые сложно отменить или которые могут повредить хост-систему.

### `block-sudo`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любую команду `sudo`.

Блокирует вызовы, содержащие ключевое слово `sudo`. Сопоставление шаблонов выполняется на разобранных токенах команды, а не на необработанной строке, чтобы предотвратить обход через инъекцию оператора оболочки.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Точные префиксы команд, которые разрешены. Каждый элемент сопоставляется с разобранными токенами argv. |

**Пример:**

```json
{
  "policyParams": {
    "block-sudo": {
      "allowPatterns": ["sudo systemctl status", "sudo journalctl"]
    }
  }
}
```

При такой конфигурации `sudo systemctl status nginx` разрешена, но `sudo rm /etc/hosts` отклонена.

<Note>
Шаблоны сопоставляются с разобранными токенами, а не с необработанной командной строкой. Это предотвращает обход через добавленные операторы оболочки (например, `sudo systemctl status x; rm -rf /` не соответствует `sudo systemctl status *`).
</Note>

---

### `block-rm-rf`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет `rm -rf`, `rm -fr` и подобные формы рекурсивного удаления.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPaths` | `string[]` | `[]` | Пути, безопасные для рекурсивного удаления (например, `/tmp`). |

**Пример:**

```json
{
  "policyParams": {
    "block-rm-rf": {
      "allowPaths": ["/tmp", "/var/cache"]
    }
  }
}
```

---

### `block-curl-pipe-sh`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет `curl <url> | bash`, `curl <url> | sh`, `wget <url> | bash` и подобные шаблоны.

Нет параметров.

---

### `block-failproofai-commands`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет команды, которые удалили бы или отключили failproofai (например, `npm uninstall failproofai`, `failproofai policies --uninstall`).

Нет параметров.

---

## Команды инфраструктуры

Остановить агентов кодирования от запуска CLI инфраструктуры или запуска конвейеров CI/CD. Все политики в этой категории **по желанию** (`defaultEnabled: false`) — агенты, которым нужен `kubectl`, `terraform` и т. д., не будут затронуты, если вы не включите политику. При включении каждый вызов соответствующего CLI отклоняется, если команда не соответствует записи в `allowPatterns`.

Грамматика шаблонов совпадает с [`block-sudo`](#block-sudo): токены сопоставляются с разобранными argv, `*` — это подстановка для одного токена, и любая команда, содержащая автономный оператор оболочки (`&&`, `||`, `|`, `;`) или токен с встроенными метасимволами оболочки, отклоняется перед проверкой списка разрешений, чтобы предотвратить инъекции.

### `block-kubectl`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любой вызов `kubectl`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд kubectl, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-kubectl": {
      "allowPatterns": ["kubectl get *", "kubectl describe *", "kubectl logs *"]
    }
  }
}
```

При такой конфигурации `kubectl get pods` разрешена, но `kubectl apply -f deploy.yaml` отклонена.

---

### `block-terraform`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любой вызов `terraform` или `tofu` (OpenTofu).

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд terraform/tofu, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-terraform": {
      "allowPatterns": ["terraform plan", "terraform validate", "terraform show *"]
    }
  }
}
```

---

### `block-aws-cli`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любой вызов AWS CLI.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд AWS CLI, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-aws-cli": {
      "allowPatterns": ["aws s3 ls *", "aws sts get-caller-identity"]
    }
  }
}
```

---

### `block-gcloud`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любой вызов `gcloud` (Google Cloud) CLI.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд gcloud, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-gcloud": {
      "allowPatterns": ["gcloud auth list", "gcloud config list"]
    }
  }
}
```

---

### `block-az-cli`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любой вызов `az` (Azure) CLI.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд az CLI, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-az-cli": {
      "allowPatterns": ["az account show", "az group list"]
    }
  }
}
```

---

### `block-helm`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет любой вызов `helm`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд helm, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-helm": {
      "allowPatterns": ["helm list", "helm status *"]
    }
  }
}
```

---

### `block-gh-pipeline`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет следующие подкоманды `gh` CLI, которые изменяют состояние или запускают конвейеры:

- `gh workflow run`, `gh workflow enable`, `gh workflow disable`
- `gh run rerun`, `gh run cancel`
- `gh pr merge`
- `gh release create`, `gh release delete`
- `gh cache delete`
- `gh secret set`, `gh secret delete`

Подкоманды `gh` только для чтения, такие как `gh pr view`, `gh pr list`, `gh run list`, `gh release view` и `gh api repos/.../...`, **не** соответствуют этой политике — они регулярно нужны для проверок рабочего процесса (включая собственный `require-ci-green-before-stop` failproofai).

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Конкретные скриптовые вызовы, которые разрешить, несмотря на то что они обычно были бы отклонены. |

**Пример:**

```json
{
  "policyParams": {
    "block-gh-pipeline": {
      "allowPatterns": ["gh run rerun *"]
    }
  }
}
```

---

## Секреты (санитайзеры)

Остановить агентов от утечки учетных данных в их контекст или выходные данные. Политики санитайзеров срабатывают на событиях **PostToolUse**. Когда Claude запускает команду Bash, читает файл или вызывает любой инструмент, эти политики проверяют выходные данные перед возвратом их Claude. Если обнаружен секретный шаблон, политика возвращает решение deny, которое предотвращает передачу выходных данных обратно.

### `sanitize-jwt`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Скрывает JWT токены (три сегмента base64url, разделённые `.`).

Нет параметров.

---

### `sanitize-api-keys`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Скрывает распространённые форматы API ключей: Anthropic (`sk-ant-`), OpenAI (`sk-`), GitHub PATs (`ghp_`), AWS access keys (`AKIA`), Stripe keys (`sk_live_`, `sk_test_`) и Google API keys (`AIza`).

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `additionalPatterns` | `{ regex: string; label: string }[]` | `[]` | Дополнительные regex шаблоны, которые следует рассматривать как секреты. |

**Пример:**

```json
{
  "policyParams": {
    "sanitize-api-keys": {
      "additionalPatterns": [
        { "regex": "myco_[A-Za-z0-9]{32}", "label": "MyCo internal API key" },
        { "regex": "pat_[0-9a-f]{40}", "label": "Internal PAT" }
      ]
    }
  }
}
```

---

### `sanitize-connection-strings`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Скрывает строки подключения к базе данных, которые содержат встроенные учетные данные (например, `postgresql://user:password@host/db`).

Нет параметров.

---

### `sanitize-private-key-content`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Скрывает PEM блоки (`-----BEGIN PRIVATE KEY-----`, `-----BEGIN RSA PRIVATE KEY-----` и т. д.).

Нет параметров.

---

### `sanitize-bearer-tokens`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Скрывает заголовки `Authorization: Bearer <token>`, где токен содержит 20 или более символов.

Нет параметров.

---

## Окружение

Защитить конфиденциальную конфигурацию окружения от чтения или разглашения агентами.

### `block-env-files`

**Событие:** PreToolUse (Bash, Read)  
**По умолчанию:** Отклоняет чтение файлов `.env` через `cat .env`, вызовы инструмента Read с `.env` в качестве пути к файлу и т. д.

Не блокирует `.envrc` или другие окружающие файлы — только файлы с названием ровно `.env`.

Нет параметров.

---

### `protect-env-vars`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет команды, которые выводят переменные окружения: `printenv`, `env`, `echo $VAR`.

Нет параметров.

---

## Доступ к файлам

Держать агентов в пределах проекта и вдали от конфиденциальных файлов.

### `block-read-outside-cwd`

**Событие:** PreToolUse (Read, Bash)  
**По умолчанию:** Отклоняет чтение файлов вне корня проекта. Граница — это `CLAUDE_PROJECT_DIR` (установленный один раз за сеанс Claude Code), с переходом на текущий рабочий каталог сеанса, когда эта переменная не установлена. Использование корня проекта вместо живого `cwd` означает, что граница остаётся стабильной даже после того, как Claude переходит в подкаталог.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPaths` | `string[]` | `[]` | Префиксы абсолютных путей, которые разрешены даже если они вне корня проекта. |

**Пример:**

```json
{
  "policyParams": {
    "block-read-outside-cwd": {
      "allowPaths": ["/shared/data", "/opt/company/config"]
    }
  }
}
```

---

### `block-secrets-write`

**Событие:** PreToolUse (Write, Edit)  
**По умолчанию:** Отклоняет записи в файлы, обычно используемые для приватных ключей и сертификатов: `id_rsa`, `id_ed25519`, `*.key`, `*.pem`, `*.p12`, `*.pfx`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `additionalPatterns` | `string[]` | `[]` | Дополнительные шаблоны имён файлов (glob-style) для блокировки. |

**Пример:**

```json
{
  "policyParams": {
    "block-secrets-write": {
      "additionalPatterns": [".token", ".secret"]
    }
  }
}
```

---

## Git

Предотвратить случайные push'и, force-push'и и ошибки ветвления, которые сложно отменить.

### `block-push-master`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет `git push origin main` и `git push origin master`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `protectedBranches` | `string[]` | `["main", "master"]` | Названия веток, на которые нельзя напрямую делать push. |

**Пример:**

```json
{
  "policyParams": {
    "block-push-master": {
      "protectedBranches": ["main", "master", "release", "prod"]
    }
  }
}
```

<Tip>
Чтобы разрешить push на все ветки (фактически отключив эту политику без удаления её из `enabledPolicies`), установите `protectedBranches: []`.
</Tip>

---

### `block-work-on-main`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет `git commit`, `git merge`, `git rebase` и `git cherry-pick` во время нахождения рабочего дерева на `main` или `master`. Создание веток и переключение (`git checkout`, `git checkout -b`, `git switch`, `git switch -c`) не затрагиваются.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `protectedBranches` | `string[]` | `["main", "master"]` | Названия веток, на которых commit/merge/rebase/cherry-pick отклоняются. |

---

### `block-force-push`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отклоняет `git push --force` и `git push -f`.

Нет специфичных параметров политики. Используйте сквозной [`hint`](/ru/configuration#hint-cross-cutting), чтобы предложить альтернативы:

```json
{
  "policyParams": {
    "block-force-push": {
      "hint": "Create a new branch from your current HEAD (e.g. `git checkout -b <new-branch>`) and push that instead."
    }
  }
}
```

---

### `warn-git-amend`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude действовать осторожно при запуске `git commit --amend`. Не блокирует команду.

Нет параметров.

---

### `warn-git-stash-drop`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude подтвердить перед запуском `git stash drop`. Не блокирует команду.

Нет параметров.

---

### `warn-all-files-staged`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude проверить, что он staging'ит, при запуске `git add -A` или `git add .`. Не блокирует команду.

Нет параметров.

---

## База данных

Перехватить деструктивные SQL операции перед их выполнением на вашей базе данных.

### `warn-destructive-sql`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude подтвердить перед запуском SQL, содержащего `DROP TABLE`, `DROP DATABASE` или `DELETE` без `WHERE` предложения.

Нет параметров.

---

### `warn-schema-alteration`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude подтвердить перед запуском `ALTER TABLE` операторов.

Нет параметров.

---

## Предупреждения

Дать агентам дополнительный контекст перед потенциально рискованными, но неразрушающими операциями.

### `warn-large-file-write`

**Событие:** PreToolUse (Write)  
**По умолчанию:** Наставляет Claude подтвердить перед записью файлов больше 1024 КБ.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `thresholdKb` | `number` | `1024` | Пороговый размер файла в килобайтах, выше которого выдаётся предупреждение. |

**Пример:**

```json
{
  "policyParams": {
    "warn-large-file-write": {
      "thresholdKb": 256
    }
  }
}
```

<Note>
Обработчик хука обеспечивает ограничение stdin в 1 МБ на нагрузки. Чтобы протестировать эту политику с малым содержимым, установите `thresholdKb` на значение намного ниже 1024.
</Note>

---

### `warn-package-publish`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude подтвердить перед запуском `npm publish`.

Нет параметров.

---

### `warn-background-process`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude быть осторожным при запуске фоновых процессов через `nohup`, `&`, `disown` или `screen`.

Нет параметров.

---

### `warn-global-package-install`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Наставляет Claude подтвердить перед запуском `npm install -g`, `yarn global add` или `pip install` без виртуального окружения.

Нет параметров.

---

## Менеджеры пакетов

Обеспечить, какие менеджеры пакетов разрешены агенту.

### `prefer-package-manager`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отключена. При включении блокирует любую команду менеджера пакетов, не в списке `allowed`, и говорит Claude переписать команду, используя разрешённый менеджер.

Обнаруживает: pip, pip3, python -m pip, npm, npx, yarn, pnpm, pnpx, bun, bunx, uv, poetry, pipenv, conda, cargo.

| Параметр | Тип | По умолчанию | Описание |
|-----------|------|---------|-------------|
| `allowed` | string[] | `[]` | Разрешённые названия менеджеров пакетов. Любой обнаруженный менеджер, не в этом списке, блокируется. Когда пусто, политика не имеет эффекта. |
| `blocked` | string[] | `[]` | Дополнительные названия менеджеров для блокировки помимо встроенного списка (например, `['pdm', 'pipx']`). |

Встроенный список блокировки охватывает: pip, pip3, npm, npx, yarn, pnpm, pnpx, bun, bunx, uv, poetry, pipenv, conda, cargo. Используйте `blocked` для добавления менеджеров, не в этом списке.

**Пример конфигурации:**

```json
{
  "enabledPolicies": ["prefer-package-manager"],
  "policyParams": {
    "prefer-package-manager": {
      "allowed": ["uv", "bun"],
      "blocked": ["pdm", "pipx"]
    }
  }
}
```

При такой конфигурации `pip install flask` и `pdm install flask` оба отклонены с сообщением, говорящим Claude использовать `uv` или `bun`. Команды как `uv pip install flask` разрешены, потому что `uv` в списке разрешений и проверяется первым.

---

## Поведение AI

Обнаружить когда агенты застревают или ведут себя неожиданно.

### `warn-repeated-tool-calls`

**Событие:** PreToolUse (все инструменты)  
**По умолчанию:** Наставляет Claude пересмотреть, когда один и тот же инструмент вызывается 3+ раза с идентичными параметрами — частый знак того, что агент застрял в цикле.

Нет параметров.

---

## Рабочий процесс

Обеспечить дисциплинированный рабочий процесс конца сеанса. Эти политики срабатывают на событии **Stop** и отклоняют возможность остановки агента до выполнения каждого условия. Они следуют естественной цепочке зависимостей: commit → push → PR → CI. Если политика отклоняет, последующие политики в цепочке пропускаются (отклонение сокращает).

Все политики рабочего процесса **fail-open**: если требуемый инструмент недоступен (например, `gh` не установлен, нет git remot'а), политика разрешает с информационным сообщением, объясняющим, почему проверка была пропущена.

### Per-CLI Stop семантика

Применение Stop выглядит немного по-разному в семи поддерживаемых CLI, потому что каждый предоставляет различный контракт хука «агент закончил». **Результат** одинаковый — агент не уходит, останавливаясь при падении ворот рабочего процесса — но **механика** отличается. Таблица ниже резюмирует; только Pi имеет видимую пользователю особенность, стоящую понимать перед включением политики `require-*-before-stop`.

| CLI | Когда срабатывают ворота | Что вы видите |
|---|---|---|
| Claude Code | Тот же цикл агента, немедленно | Claude продолжает работать — исправляет проблему, потом снова пытается закончить. Никакого перерыва для вас видно. |
| Codex | Тот же цикл агента, немедленно | То же, что Claude. |
| GitHub Copilot CLI | Тот же цикл агента, немедленно | То же, что Claude (использует канал повтора `{decision:"block", reason}` Copilot — проверено эмпирически на Copilot CLI 1.0.41). |
| Cursor Agent | Тот же цикл агента, немедленно | То же, что Claude (использует канал `{followup_message}` Cursor — ограничено `loop_limit`, по умолчанию 5 повторов). |
| Gemini CLI | Тот же цикл агента, немедленно | То же, что Claude (использует канал `{decision:"block", reason}` Gemini на `AfterAgent`). |
| OpenCode | Тот же цикл агента, немедленно | То же, что Claude (использует вызов SDK `client.session.prompt(...)` OpenCode, маршрутизированный через `hookSpecificOutput.additionalContext`). |
| **Pi (pi-coding-agent)** | **Следующий ход пользователя** | **Pi видимо остановится** при срабатывании ворот — его цикл агента выходит и вам возвращается приглашение. Ворота потом срабатывают при следующем отправлении приглашения: failproofai добавляет директиву `MANDATORY ACTION REQUIRED` в начало системного приглашения того хода, наставляя LLM завершить этап рабочего процесса (commit, push и т. д.) перед выполнением того, что вы попросили. |

<Note>
**Ограничение Pi.** Pi's `AgentEndEvent` (эквивалент `Stop` хука Claude в восходящей цепочке) не имеет типа Result — к тому времени, как он срабатывает, цикл агента Pi уже вышел. Pi не может быть принужден повторить тот же цикл, как Claude / Copilot / Cursor / Gemini / OpenCode. failproofai переводит ворота на событие `before_agent_start` Pi (которое срабатывает после следующего приглашения пользователя), поэтому проверка рабочего процесса всё ещё применяется, просто на следующем ходу вместо текущего.

**Что это означает на практике:**

- После остановки Pi, причина отклонения захватывается в памяти по ID сеанса Pi. Следующий промпт, который вы отправляете в том же процессе Pi, дренирует его: LLM видит директиву `MANDATORY ACTION REQUIRED` в начале системного приглашения, делает commit (или push / открывает PR / ждёт CI) и только потом продолжает с вашим запросом. Захваченная причина отклонения является одноразовой — один раз дренирована, ворота чистые.
- Ворота ограничены временем жизни процесса Pi. Если вы сделаете `Ctrl+C` Pi или выйдете между ходами, запись в памяти теряется вместе с процессом и ворота упускаются. Claude, Copilot, Cursor, Gemini и OpenCode имеют ту же границу (убить агента и ворота упускаются) — Pi просто делает это более видимым, потому что агент видимо выходит перед срабатыванием ворот.
- Ожидающее отклонение также очищается на `session_shutdown` по любой причине (`new` / `resume` / `fork` / `quit`), поэтому устаревшие ворота из предыдущего сеанса не могут просочиться в свежий сеанс, начатый в том же процессе Pi.

Если вам нужна одноцикловая повторная попытка стиля Claude, запустите ваши политики `Stop` на любом из других шести поддерживаемых CLI. Мы отслеживаем Pi в восходящей цепочке для будущего типа Result на `AgentEndEvent`, который позволил бы нам закрыть эту щель.
</Note>

### `require-commit-before-stop`

**Событие:** Stop  
**По умолчанию:** Отклоняет остановку при наличии неподготовленных изменений (изменённые, staged или неслеживаемые файлы). Возвращает информационное сообщение когда рабочий каталог чистый.

Нет параметров.

---

### `require-push-before-stop`

**Событие:** Stop  
**По умолчанию:** Отклоняет остановку при наличии неотправленных commit'ов или когда текущая ветка не имеет отслеживаемой удалённой ветки. Предлагает `git push -u` для создания отслеживаемой ветки при необходимости. Fail open если удалённый не настроен.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `remote` | `string` | `"origin"` | Имя удалённого для push'а. |

**Пример:**

```json
{
  "policyParams": {
    "require-push-before-stop": {
      "remote": "upstream"
    }
  }
}
```

---

### `require-pr-before-stop`

**Событие:** Stop  
**По умолчанию:** Отклоняет остановку, когда не существует pull request для текущей ветки, или когда существующий PR закрыт без слияния. Наставляет Claude создать PR с `gh pr create`. Когда PR **слит**, политика разрешает (работа доставлена) и сообщение намекает на переключение с ветки (`git checkout main && git pull`).

Нет параметров.

<Note>
Эта политика требует установку [GitHub CLI](https://cli.github.com/) (`gh`) и аутентификацию.
Запустите `gh auth login` с личным токеном доступа, имеющим область `repo` для чтения доступа к
pull request'ам. Если `gh` не установлен или не аутентифицирован, политика fail open и сообщит причину Claude.
</Note>

---

### `require-no-conflicts-before-stop`

**Событие:** Stop  
**По умолчанию:** Отклоняет остановку, когда текущая ветка не может чисто слиться в базовую ветку. Политика сначала подтверждает наличие `OPEN` PR на GitHub для ветки — без одного, нет целевого назначения для применения, поэтому вся политика сокращает к разрешению. Как только `OPEN` PR подтверждён, запускаются два независимых зонда:

1. **Локальный** — `git merge-tree --write-tree --name-only origin/<baseBranch> HEAD`. При конфликте сообщение отклонения называет конфликтующие файлы, поэтому Claude знает ровно что разрешить.
2. **GitHub** — переиспользует результат `gh pr view --json mergeable,state`, уже полученный в предпроверке. Ловит конфликты, которые устаревший локальный `origin/<baseBranch>` пропустит бы (например, кто-то посадил конфликтующий PR на `main` со времени последней fetch). Результат `CONFLICTING` отклоняет. Результат `UNKNOWN` также отклоняет и наставляет Claude подождать ~10 секунд и переучитать перед попыткой остановиться снова — это предотвращает ложные отрицания пока GitHub пересчитывает.

Пропускает полностью (разрешает) когда: `gh` не установлен, не существует PR для ветки, состояние PR не `OPEN` (например, `MERGED`, `CLOSED`), или `gh pr view` возвращает непроанализируемый выход. Также fail open когда `origin/<baseBranch>` отсутствует локально или когда нет commit'ов впереди base — те Layer 1 fall-through'и всё ещё консультируют кэшированную mergeability PR перед разрешением.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `baseBranch` | `string` | `"main"` | Базовая ветка для проверки конфликтов. |

<Note>
GitHub CLI (`gh`) требуется для этой политики. Политика использует `gh pr view` для подтверждения
существования `OPEN` PR перед запуском любого зонда конфликта — без `gh`, политика
сокращает к разрешению. Запустите `gh auth login` с личным токеном доступа, имеющим
область `repo` для чтения доступа к pull request'ам.
</Note>

---

### `require-ci-green-before-stop`

**Событие:** Stop  
**По умолчанию:** Отклоняет остановку когда CI проверки падают или всё ещё запущены на текущей ветке. Проверяет оба потока GitHub Actions и проверки третьих лиц (например, CodeRabbit, SonarCloud, Codecov). Рассматривает `skipped` и `cancelled` заключения как успех. Возвращает информационное сообщение когда все проверки проходят.

Нет параметров.

<Note>
Эта политика требует установку [GitHub CLI](https://cli.github.com/) (`gh`) и аутентификацию.
Запустите `gh auth login` с личным токеном доступа, имеющим область `repo` для чтения доступа к
потокам Actions и Checks API. Если `gh` не установлен или не аутентифицирован, политика fail open и сообщит причину Claude.
</Note>

---

---

## Отключение отдельных политик

Удалить конкретную политику из `enabledPolicies` в вашей конфигурации, или переключить её в закладке Policies в dashboard.

```json
{
  "enabledPolicies": [
    "block-rm-rf",
    "sanitize-api-keys"
  ]
}
```

Политики, не перечисленные в `enabledPolicies`, не запускаются, даже если существуют для них записи `policyParams`.