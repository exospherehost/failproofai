---
title: Встроенные политики
description: "Все 39 встроенных политик, которые перехватывают распространённые режимы отказа агентов"
icon: shield
---

failproofai поставляется с 39 встроенными политиками, которые перехватывают распространённые режимы отказа агентов. Каждая политика срабатывает на определённый тип события хука и имя инструмента. Девятнадцать политик принимают параметры, которые позволяют настроить их поведение без написания кода. Пять рабочих политик обеспечивают конвейер commit → push → PR → CI, прежде чем Claude остановится.

---

## Обзор

Политики сгруппированы по категориям:

| Категория | Политики | Тип хука |
|----------|----------|-----------|
| [Опасные команды](#опасные-команды) | block-sudo, block-rm-rf, block-curl-pipe-sh, block-failproofai-commands | PreToolUse |
| [Команды инфраструктуры](#команды-инфраструктуры) | block-kubectl, block-terraform, block-aws-cli, block-gcloud, block-az-cli, block-helm, block-gh-pipeline | PreToolUse |
| [Секреты (санитайзеры)](#секреты-санитайзеры) | sanitize-jwt, sanitize-api-keys, sanitize-connection-strings, sanitize-private-key-content, sanitize-bearer-tokens | PostToolUse |
| [Окружение](#окружение) | block-env-files, protect-env-vars | PreToolUse |
| [Доступ к файлам](#доступ-к-файлам) | block-read-outside-cwd, block-secrets-write | PreToolUse |
| [Git](#git) | block-push-master, block-work-on-main, block-force-push, warn-git-amend, warn-git-stash-drop, warn-all-files-staged | PreToolUse |
| [База данных](#база-данных) | warn-destructive-sql, warn-schema-alteration | PreToolUse |
| [Предупреждения](#предупреждения) | warn-large-file-write, warn-package-publish, warn-background-process, warn-global-package-install | PreToolUse |
| [Менеджеры пакетов](#менеджеры-пакетов) | prefer-package-manager | PreToolUse |
| [Рабочий процесс](#рабочий-процесс) | require-commit-before-stop, require-push-before-stop, require-pr-before-stop, require-no-conflicts-before-stop, require-ci-green-before-stop | Stop |

- **`block-`** — остановить выполнение агентом.
- **`warn-`** — предоставить агенту дополнительный контекст, чтобы он мог самовыправиться.
- **`sanitize-`** — удалить конфиденциальные данные из выходных данных инструмента перед отправкой агенту.

### Пространства имён

Каждая политика находится в слоте `<namespace>/<name>`. Встроенные политики принадлежат к пространству имён **`failproofai/`** — например, `failproofai/sanitize-jwt`. Пространство имён предотвращает столкновения при загрузке пользовательских или сторонних политик с похожими короткими именами.

В вашей конфигурации вы можете ссылаться на встроенную политику либо по её короткому имени, либо по полному имени; обе формы разрешаются в одну политику:

```json
{
  "enabledPolicies": [
    "sanitize-jwt",
    "failproofai/block-rm-rf"
  ]
}
```

Если имя не содержит `/`, failproofai рассматривает его как принадлежащее пространству имён по умолчанию `failproofai`. Имена, уже содержащие `/` (например `myorg/foo`, `custom/my-hook`), сохраняются как есть.
- **`require-`** — заблокировать событие Stop до выполнения условий.

---

<Tip>
Каждая политика поддерживает опциональное поле `hint` в `policyParams`. Подсказка добавляется к сообщению deny или instruct, которое видит Claude, обеспечивая практическое руководство без изменения кода политики. Работает со встроенными, пользовательскими и условными политиками. Подробнее в [Configuration → hint](/ru/configuration#hint-cross-cutting).
</Tip>

---

## Опасные команды

Предотвращайте выполнение агентами операций, которые сложно отменить или которые могут повредить хост-систему.

### `block-sudo`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любой команде с `sudo`.

Блокирует вызовы, содержащие ключевое слово `sudo`. Сопоставление паттернов выполняется с разбором токенов команды, а не с необработанной строкой, чтобы предотвратить обход через инъекцию оператора оболочки.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Точные префиксы команд, которые разрешены. Каждая запись сопоставляется с разбранными токенами argv. |

**Пример:**

```json
{
  "policyParams": {
    "block-sudo": {
      "allowPatterns": ["sudo systemctl status", "sudo journalctl"]
    }
  }
}
```

С такой конфигурацией `sudo systemctl status nginx` разрешен, но `sudo rm /etc/hosts` отказан.

<Note>
Паттерны сопоставляются с разбранными токенами, а не с необработанной строкой команды. Это предотвращает обход через добавленные операторы оболочки (например `sudo systemctl status x; rm -rf /` не соответствует `sudo systemctl status *`).
</Note>

---

### `block-rm-rf`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает `rm -rf`, `rm -fr` и аналогичные формы рекурсивного удаления.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPaths` | `string[]` | `[]` | Пути, которые безопасно рекурсивно удалять (например `/tmp`). |

**Пример:**

```json
{
  "policyParams": {
    "block-rm-rf": {
      "allowPaths": ["/tmp", "/var/cache"]
    }
  }
}
```

---

### `block-curl-pipe-sh`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает `curl <url> | bash`, `curl <url> | sh`, `wget <url> | bash` и аналогичные паттерны.

Параметры отсутствуют.

---

### `block-failproofai-commands`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает команды, которые отключили бы или удалили бы сам failproofai (например `npm uninstall failproofai`, `failproofai policies --uninstall`).

Параметры отсутствуют.

---

## Команды инфраструктуры

Остановите кодирующих агентов от запуска инфраструктурных CLI или запуска конвейеров CI/CD. Все политики в этой категории **opt-in** (`defaultEnabled: false`) — агенты, которым действительно нужно вызывать `kubectl`, `terraform` и т. д., не будут нарушены, если вы не включите политику. Если включено, каждый вызов совпадающего CLI отказан, если только команда не совпадает с записью в `allowPatterns`.

Грамматика паттернов такая же, как в [`block-sudo`](#block-sudo): токены сопоставляются с разбранными argv, `*` — это подстановочный знак для одного токена, и любая команда, содержащая автономный оператор оболочки (`&&`, `||`, `|`, `;`) или токен с встроенными метасимволами оболочки, отклоняется до проверки списка разрешений, чтобы предотвратить инъекции.

### `block-kubectl`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любом вызове `kubectl`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд kubectl, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-kubectl": {
      "allowPatterns": ["kubectl get *", "kubectl describe *", "kubectl logs *"]
    }
  }
}
```

С такой конфигурацией `kubectl get pods` разрешен, но `kubectl apply -f deploy.yaml` отказан.

---

### `block-terraform`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любом вызове `terraform` или `tofu` (OpenTofu).

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд terraform/tofu, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-terraform": {
      "allowPatterns": ["terraform plan", "terraform validate", "terraform show *"]
    }
  }
}
```

---

### `block-aws-cli`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любом вызове `aws` CLI.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд aws CLI, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-aws-cli": {
      "allowPatterns": ["aws s3 ls *", "aws sts get-caller-identity"]
    }
  }
}
```

---

### `block-gcloud`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любом вызове `gcloud` (Google Cloud) CLI.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд gcloud, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-gcloud": {
      "allowPatterns": ["gcloud auth list", "gcloud config list"]
    }
  }
}
```

---

### `block-az-cli`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любом вызове `az` (Azure) CLI.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд az CLI, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-az-cli": {
      "allowPatterns": ["az account show", "az group list"]
    }
  }
}
```

---

### `block-helm`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает в любом вызове `helm`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Префиксы команд helm, которые разрешены. |

**Пример:**

```json
{
  "policyParams": {
    "block-helm": {
      "allowPatterns": ["helm list", "helm status *"]
    }
  }
}
```

---

### `block-gh-pipeline`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает следующим подкомандам `gh` CLI, которые изменяют состояние или запускают конвейеры:

- `gh workflow run`, `gh workflow enable`, `gh workflow disable`
- `gh run rerun`, `gh run cancel`
- `gh pr merge`
- `gh release create`, `gh release delete`
- `gh cache delete`
- `gh secret set`, `gh secret delete`

Подкоманды `gh` только для чтения, такие как `gh pr view`, `gh pr list`, `gh run list`, `gh release view` и `gh api repos/.../...`, **не** соответствуют этой политике — они регулярно требуются для проверок рабочего процесса (включая собственную `require-ci-green-before-stop` failproofai).

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPatterns` | `string[]` | `[]` | Конкретные запрограммированные вызовы, которые разрешены даже если они иначе будут отказаны. |

**Пример:**

```json
{
  "policyParams": {
    "block-gh-pipeline": {
      "allowPatterns": ["gh run rerun *"]
    }
  }
}
```

---

## Секреты (санитайзеры)

Остановите агентов от утечки учётных данных в их контекст или вывод. Политики санитайзеров срабатывают на событиях **PostToolUse**. Когда Claude запускает команду Bash, читает файл или вызывает любой инструмент, эти политики проверяют выходные данные перед их возвращением Claude. Если обнаружен паттерн секрета, политика возвращает решение deny, которое предотвращает передачу выходных данных обратно.

### `sanitize-jwt`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Удаляет JWT токены (три сегмента base64url, разделённые `.`).

Параметры отсутствуют.

---

### `sanitize-api-keys`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Удаляет распространённые форматы API ключей: Anthropic (`sk-ant-`), OpenAI (`sk-`), GitHub PATs (`ghp_`), AWS access keys (`AKIA`), Stripe keys (`sk_live_`, `sk_test_`), и Google API keys (`AIza`).

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `additionalPatterns` | `{ regex: string; label: string }[]` | `[]` | Дополнительные regex паттерны, которые следует рассматривать как секреты. |

**Пример:**

```json
{
  "policyParams": {
    "sanitize-api-keys": {
      "additionalPatterns": [
        { "regex": "myco_[A-Za-z0-9]{32}", "label": "MyCo internal API key" },
        { "regex": "pat_[0-9a-f]{40}", "label": "Internal PAT" }
      ]
    }
  }
}
```

---

### `sanitize-connection-strings`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Удаляет строки подключения к базе данных, содержащие встроенные учётные данные (например `postgresql://user:password@host/db`).

Параметры отсутствуют.

---

### `sanitize-private-key-content`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Удаляет PEM блоки (`-----BEGIN PRIVATE KEY-----`, `-----BEGIN RSA PRIVATE KEY-----` и т. д.).

Параметры отсутствуют.

---

### `sanitize-bearer-tokens`

**Событие:** PostToolUse (все инструменты)  
**По умолчанию:** Удаляет заголовки `Authorization: Bearer <token>`, где токен содержит 20 или более символов.

Параметры отсутствуют.

---

## Окружение

Защитите конфиденциальную конфигурацию окружения от чтения или разоблачения агентами.

### `block-env-files`

**Событие:** PreToolUse (Bash, Read)  
**По умолчанию:** Отказывает чтению файлов `.env` через `cat .env`, вызовы инструмента Read с `.env` в качестве пути файла и т. д.

Не блокирует `.envrc` или другие файлы, связанные с окружением — только файлы, названные точно `.env`.

Параметры отсутствуют.

---

### `protect-env-vars`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает командам, которые выводят переменные окружения: `printenv`, `env`, `echo $VAR`.

Параметры отсутствуют.

---

## Доступ к файлам

Держите агентов работающими в границах проекта и вдали от конфиденциальных файлов.

### `block-read-outside-cwd`

**Событие:** PreToolUse (Read, Bash)  
**По умолчанию:** Отказывает чтению файлов вне корня проекта. Граница — это `CLAUDE_PROJECT_DIR` (установленная один раз за сеанс Claude Code), с отступом на текущий рабочий каталог сеанса, когда эта переменная не установлена. Использование корня проекта вместо живого `cwd` означает, что граница остаётся стабильной даже после того, как Claude перейдёт в подкаталог.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `allowPaths` | `string[]` | `[]` | Абсолютные префиксы путей, которые разрешены даже если находятся вне корня проекта. |

**Пример:**

```json
{
  "policyParams": {
    "block-read-outside-cwd": {
      "allowPaths": ["/shared/data", "/opt/company/config"]
    }
  }
}
```

---

### `block-secrets-write`

**Событие:** PreToolUse (Write, Edit)  
**По умолчанию:** Отказывает записи в файлы, обычно используемые для приватных ключей и сертификатов: `id_rsa`, `id_ed25519`, `*.key`, `*.pem`, `*.p12`, `*.pfx`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `additionalPatterns` | `string[]` | `[]` | Дополнительные паттерны имён файлов (в стиле glob) для блокировки. |

**Пример:**

```json
{
  "policyParams": {
    "block-secrets-write": {
      "additionalPatterns": [".token", ".secret"]
    }
  }
}
```

---

## Git

Предотвращайте случайные push, force-push и ошибки ветвления, которые сложно отменить.

### `block-push-master`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает `git push origin main` и `git push origin master`.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `protectedBranches` | `string[]` | `["main", "master"]` | Имена ветвей, которые нельзя push'ить напрямую. |

**Пример:**

```json
{
  "policyParams": {
    "block-push-master": {
      "protectedBranches": ["main", "master", "release", "prod"]
    }
  }
}
```

<Tip>
Чтобы разрешить push'ить на все ветви (эффективно отключив эту политику без удаления её из `enabledPolicies`), установите `protectedBranches: []`.
</Tip>

---

### `block-work-on-main`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает `git commit`, `git merge`, `git rebase` и `git cherry-pick` во время работы дерева на `main` или `master`. Создание и переключение ветвей (`git checkout`, `git checkout -b`, `git switch`, `git switch -c`) не затрагиваются.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `protectedBranches` | `string[]` | `["main", "master"]` | Имена ветвей, на которых commit/merge/rebase/cherry-pick отказан. |

---

### `block-force-push`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отказывает `git push --force` и `git push -f`.

Политика-специфичных параметров отсутствует. Используйте кросс-сквозную [`hint`](/ru/configuration#hint-cross-cutting) для предложения альтернатив:

```json
{
  "policyParams": {
    "block-force-push": {
      "hint": "Create a new branch from your current HEAD (e.g. `git checkout -b <new-branch>`) and push that instead."
    }
  }
}
```

---

### `warn-git-amend`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude действовать осторожно при запуске `git commit --amend`. Не блокирует команду.

Параметры отсутствуют.

---

### `warn-git-stash-drop`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude подтвердить перед запуском `git stash drop`. Не блокирует команду.

Параметры отсутствуют.

---

### `warn-all-files-staged`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude проверить что он ставит на сцену при запуске `git add -A` или `git add .`. Не блокирует команду.

Параметры отсутствуют.

---

## База данных

Перехватите деструктивные SQL операции перед их выполнением против базы данных.

### `warn-destructive-sql`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude подтвердить перед запуском SQL, содержащего `DROP TABLE`, `DROP DATABASE` или `DELETE` без `WHERE` предложения.

Параметры отсутствуют.

---

### `warn-schema-alteration`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude подтвердить перед запуском утверждений `ALTER TABLE`.

Параметры отсутствуют.

---

## Предупреждения

Предоставьте агентам дополнительный контекст перед потенциально рискованными, но не деструктивными операциями.

### `warn-large-file-write`

**Событие:** PreToolUse (Write)  
**По умолчанию:** Инструктирует Claude подтвердить перед записью файлов больше 1024 КБ.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `thresholdKb` | `number` | `1024` | Порог размера файла в килобайтах, выше которого выдаётся предупреждение. |

**Пример:**

```json
{
  "policyParams": {
    "warn-large-file-write": {
      "thresholdKb": 256
    }
  }
}
```

<Note>
Обработчик хука обеспечивает ограничение stdin в 1 МБ для полезных нагрузок. Для тестирования этой политики с небольшим содержимым установите `thresholdKb` на значение значительно ниже 1024.
</Note>

---

### `warn-package-publish`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude подтвердить перед запуском `npm publish`.

Параметры отсутствуют.

---

### `warn-background-process`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude быть осторожным при запуске фоновых процессов через `nohup`, `&`, `disown` или `screen`.

Параметры отсутствуют.

---

### `warn-global-package-install`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Инструктирует Claude подтвердить перед запуском `npm install -g`, `yarn global add` или `pip install` без виртуального окружения.

Параметры отсутствуют.

---

## Менеджеры пакетов

Обеспечьте, какие менеджеры пакетов может использовать агент.

### `prefer-package-manager`

**Событие:** PreToolUse (Bash)  
**По умолчанию:** Отключено. Если включено, блокирует любую команду менеджера пакетов, которая не находится в списке `allowed`, и говорит Claude переписать команду, используя разрешённый менеджер.

Обнаруживает: pip, pip3, python -m pip, npm, npx, yarn, pnpm, pnpx, bun, bunx, uv, poetry, pipenv, conda, cargo.

| Параметр | Тип | По умолчанию | Описание |
|-----------|------|---------|-------------|
| `allowed` | string[] | `[]` | Разрешённые имена менеджеров пакетов. Любой обнаруженный менеджер, не входящий в этот список, блокируется. Если пусто, политика неэффективна. |
| `blocked` | string[] | `[]` | Дополнительные имена менеджеров для блокировки сверх встроенного списка (например `['pdm', 'pipx']`). |

Встроенный список блокировки охватывает: pip, pip3, npm, npx, yarn, pnpm, pnpx, bun, bunx, uv, poetry, pipenv, conda, cargo. Используйте `blocked` для добавления менеджеров, отсутствующих в этом списке.

**Пример конфигурации:**

```json
{
  "enabledPolicies": ["prefer-package-manager"],
  "policyParams": {
    "prefer-package-manager": {
      "allowed": ["uv", "bun"],
      "blocked": ["pdm", "pipx"]
    }
  }
}
```

С этой конфигурацией `pip install flask` и `pdm install flask` оба отказаны с сообщением, говорящим Claude использовать `uv` или `bun`. Команды типа `uv pip install flask` разрешены, потому что `uv` находится в списке разрешений и проверяется в первую очередь.

---

## Поведение ИИ

Обнаружьте, когда агенты застревают или ведут себя неожиданно.

### `warn-repeated-tool-calls`

**Событие:** PreToolUse (все инструменты)  
**По умолчанию:** Инструктирует Claude пересмотреть, когда тот же инструмент вызывается 3+ раза с идентичными параметрами — распространённый признак, что агент застрял в цикле.

Параметры отсутствуют.

---

## Рабочий процесс

Обеспечьте дисциплинированный рабочий процесс конца сеанса. Эти политики срабатывают на событии **Stop** и отказывают агенту от остановки до выполнения каждого условия. Они следуют естественной цепочке зависимостей: commit → push → PR → CI. Если политика отказывает, более поздние политики в цепи пропускаются (deny прерывает короткую цепь).

Все рабочие политики **fail-open**: если требуемый инструмент недоступен (например `gh` не установлен, нет git remote), политика позволяет с информационным сообщением, объясняющим, почему проверка была пропущена.

### Семантика Stop для каждого CLI

Принудительное применение Stop выглядит немного по-разному на семи поддерживаемых CLI, потому что каждый имеет другой контракт хука "агент завершён". **Результат** одинаков — агент не избегает остановки при отказе рабочих ворот — но **механика** различается. Таблица ниже суммирует; только Pi имеет видимый пользователю нюанс, стоящий понимания перед включением политики `require-*-before-stop`.

| CLI | Когда срабатывают ворота | Что вы видите |
|---|---|---|
| Claude Code | Тот же цикл агента, сразу же | Claude продолжает работать — исправляет проблему, затем пытается закончить снова. Никакого прерывания видимого вам. |
| Codex | Тот же цикл агента, сразу же | Такой же как Claude. |
| GitHub Copilot CLI | Тот же цикл агента, сразу же | Такой же как Claude (использует канал повтора `{decision:"block", reason}` Copilot — проверено эмпирически против Copilot CLI 1.0.41). |
| Cursor Agent | Тот же цикл агента, сразу же | Такой же как Claude (использует канал `{followup_message}` Cursor — ограничен `loop_limit`, по умолчанию 5 повторов). |
| Gemini CLI | Тот же цикл агента, сразу же | Такой же как Claude (использует канал `{decision:"block", reason}` Gemini на `AfterAgent`). |
| OpenCode | Тот же цикл агента, сразу же | Такой же как Claude (использует вызов SDK `client.session.prompt(...)` OpenCode маршрутизированный через `hookSpecificOutput.additionalContext`). |
| **Pi (pi-coding-agent)** | **Следующий ход пользователя** | **Pi видимо останавливается**, когда срабатывают ворота — его цикл агента выходит и вы возвращаетесь в подсказку. Ворота затем срабатывают следующий раз, когда вы отправляете подсказку: failproofai предпосылает директиву `MANDATORY ACTION REQUIRED` к системной подсказке этого хода, инструктируя LLM завершить шаг рабочего процесса (commit, push и т. д.) перед выполнением того, что вы просили. |

<Note>
**Ограничение Pi.** `AgentEndEvent` Pi (эквивалент upstream `Stop` хука Claude) не имеет типа Result — к моменту его срабатывания цикл агента Pi уже выхожен. Pi не может быть принуждён повторить тот же цикл так, как Claude / Copilot / Cursor / Gemini / OpenCode. failproofai переносит ворота на событие Pi `before_agent_start` (которое срабатывает после следующей подсказки пользователя), так что проверка рабочего процесса все ещё обеспечивается, просто на следующем ходу вместо текущего.

**Что это означает на практике:**

- После остановки Pi, причина отказа захватывается в памяти по ID сеанса Pi. Самая следующая подсказка, которую вы отправляете в том же процессе Pi, её исчерпывает: LLM видит директиву `MANDATORY ACTION REQUIRED` в верхней части своей системной подсказки, коммитит (или push / открывает PR / ждёт CI), и только затем продолжает с вашим запросом. Захваченная причина отказа one-shot — один раз исчерпана, ворота清.
- Ворота ограничены временем жизни процесса Pi. Если вы `Ctrl+C` Pi или выходите между ходами, запись в памяти удаляется вместе с процессом и ворота пропущены. Claude, Copilot, Cursor, Gemini и OpenCode имеют ту же границу (убить агента и ворота пропущены) — Pi просто делает это более видимым, потому что агент видимо выходит перед срабатыванием ворот.
- Ожидающий отказ также очищается на `session_shutdown` по любой причине (`new` / `resume` / `fork` / `quit`), поэтому устаревшие ворота из предыдущего сеанса не могут утечь в свежий сеанс, запущенный в том же процессе Pi.

Если вам нужен повтор одного цикла в стиле Claude, запустите ваши политики `Stop` на любом из шести других поддерживаемых CLI. Мы отслеживаем Pi upstream для будущего типа Result на `AgentEndEvent`, который позволил бы нам закрыть этот разрыв.
</Note>

### `require-commit-before-stop`

**Событие:** Stop  
**По умолчанию:** Отказывает остановке, когда есть незакоммиченные изменения (изменённые, поставленные на сцену или неотслеживаемые файлы). Возвращает информационное сообщение, когда рабочий каталог чист.

Параметры отсутствуют.

---

### `require-push-before-stop`

**Событие:** Stop  
**По умолчанию:** Отказывает остановке, когда есть непроталкиваемые коммиты или когда текущая ветвь не имеет удалённой ветви отслеживания. Предлагает `git push -u` для создания ветви отслеживания если требуется. Отказывает gracefully, если нет настроенного удалённого.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `remote` | `string` | `"origin"` | Имя удалённого для push'ины. |

**Пример:**

```json
{
  "policyParams": {
    "require-push-before-stop": {
      "remote": "upstream"
    }
  }
}
```

---

### `require-pr-before-stop`

**Событие:** Stop  
**По умолчанию:** Отказывает остановке, когда нет pull request для текущей ветви или когда существующий PR закрыт без объединения. Инструктирует Claude создать PR с `gh pr create`. Когда PR **объединён**, политика позволяет (работа отправлена) и сообщение намекает на переключение вне ветви (`git checkout main && git pull`).

Параметры отсутствуют.

<Note>
Эта политика требует установленного и аутентифицированного [GitHub CLI](https://cli.github.com/) (`gh`).
Запустите `gh auth login` с персональным токеном доступа, имеющим область `repo` для доступа на чтение к
pull requests. Если `gh` не установлен или не аутентифицирован, политика позволяет с открытым текстом и сообщает причину Claude.
</Note>

---

### `require-no-conflicts-before-stop`

**Событие:** Stop  
**По умолчанию:** Отказывает остановке, когда текущая ветвь не может чистро объединиться в базовую ветвь. Политика сначала подтверждает наличие `OPEN` PR на GitHub для ветви — без одного, нет цели объединения для обеспечения, так что вся политика short-circuits для разрешения. После подтверждения `OPEN` PR, два независимых зонда запускаются:

1. **Локальный** — `git merge-tree --write-tree --name-only origin/<baseBranch> HEAD`. На конфликте сообщение deny называет конфликтные файлы, так что Claude знает точно что разрешить.
2. **GitHub** — переиспользует результат `gh pr view --json mergeable,state` уже получаемый в предварительной проверке. Перехватывает конфликты, которые устаревший локальный `origin/<baseBranch>` пропустил бы (например кто-то задействовал конфликтный PR на `main` с последней выборки). Результат `CONFLICTING` отказывает. Результат `UNKNOWN` также отказывает и инструктирует Claude подождать ~10 секунд и повторно проверить перед попыткой остановиться снова — это предотвращает ложные отрицания пока GitHub пересчитывает.

Полностью пропускается (позволяет) когда: `gh` не установлен, нет PR для ветви, состояние PR не `OPEN` (например `MERGED`, `CLOSED`), или `gh pr view` возвращает непарсируемый вывод. Также позволяет gracefully когда `origin/<baseBranch>` отсутствует локально или когда нет коммитов впереди base — те Layer 1 fall-throughs все ещё консультируют кэшированный PR mergeability перед разрешением.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|-------|------|---------|-------------|
| `baseBranch` | `string` | `"main"` | Базовая ветвь для проверки конфликтов. |

<Note>
GitHub CLI (`gh`) требуется для этой политики. Политика использует `gh pr view` для подтверждения
что `OPEN` PR существует перед запуском любого зонда конфликта — без `gh`, политика
short-circuits для разрешения. Запустите `gh auth login` с персональным токеном доступа, имеющим
область `repo` для доступа на чтение к pull requests.
</Note>

---

### `require-ci-green-before-stop`

**Событие:** Stop  
**По умолчанию:** Отказывает остановке, когда CI проверки падают или всё ещё работают на текущей ветви. Проверяет как GitHub Actions workflow запускает, так и сторонние бот проверки (например CodeRabbit, SonarCloud, Codecov). Рассматривает `skipped` и `cancelled` выводы как успех. Возвращает информационное сообщение, когда все проверки проходят.

Параметры отсутствуют.

<Note>
Эта политика требует установленного и аутентифицированного [GitHub CLI](https://cli.github.com/) (`gh`).
Запустите `gh auth login` с персональным токеном доступа, имеющим область `repo` для доступа на чтение к
Actions workflow запускам и Checks API. Если `gh` не установлен или не аутентифицирован, политика позволяет и сообщает причину Claude.
</Note>

---

---

## Отключение отдельных политик

Удалите конкретную политику из `enabledPolicies` в вашей конфигурации или переключите её в закладке Policies приборной панели.

```json
{
  "enabledPolicies": [
    "block-rm-rf",
    "sanitize-api-keys"
  ]
}
```

Политики, не перечисленные в `enabledPolicies`, не запускаются, даже если для них существуют записи `policyParams`.