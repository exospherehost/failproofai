---
title: Псевдонимы пакета
description: "Зарегистрированные псевдонимы для предотвращения опечаток и их принцип работы"
icon: copy
---

## Официальный пакет

Канонический пакет npm — это **`failproofai`**:

```bash
npm install -g failproofai
# или
bun add -g failproofai
```

---

## Почему мы владеем этими псевдонимами

Typosquatting — это распространённая атака на цепочку поставок, при которой злоумышленник регистрирует имя пакета, отличающееся на один символ от популярного пакета. Пользователи, допустившие ошибку при вводе команды установки, могут запустить контролируемый злоумышленником код с полным доступом к системе — именно от такого рода угроз защищает Failproof AI.

Чтобы устранить эту уязвимость, **мы заранее регистрируем все распространённые опечатки и варианты написания** `failproofai` на npm. Никто третий не сможет зарегистрировать эти имена. Каждый из них — это тонкий прокси, который устанавливает и делегирует вызовы настоящему пакету `failproofai`.

---

## Зарегистрированные псевдонимы

**Варианты написания** — разные способы написания "failproof ai":

| Пакет | Статус |
|---------|--------|
| `failproof` | ✅ Опубликован |
| `failproof-ai` | ⏳ Ожидание поддержки npm |
| `fail-proof-ai` | ⏳ Ожидание поддержки npm |
| `failproof_ai` | ⏳ Ожидание поддержки npm |
| `fail_proof_ai` | ⏳ Ожидание поддержки npm |
| `fail-proofai` | ⏳ Ожидание поддержки npm |

**Опечатки `failprof*`** — пропущена одна буква "o" в слове "proof":

| Пакет | Статус |
|---------|--------|
| `failprof` | ✅ Опубликован |
| `failprof-ai` | ✅ Опубликован |
| `failprofai` | ⏳ Ожидание поддержки npm |
| `fail-prof-ai` | ⏳ Ожидание поддержки npm |
| `failprof_ai` | ⏳ Ожидание поддержки npm |

**Опечатки `faliproof*`** — буквы "a" и "i" поменяны местами:

| Пакет | Статус |
|---------|--------|
| `faliproof` | ✅ Опубликован |
| `faliproof-ai` | ✅ Опубликован |
| `faliproofai` | ⏳ Ожидание поддержки npm |

> **Почему статус ожидание?** Политика защиты от спама npm блокирует имена, которые нормализуются в ту же строку, что и существующий пакет, после удаления пунктуации и проверки сходства. Мы связались со службой поддержки npm для зарезервирования этих имен в целях защиты от typosquatting. Они будут активированы после одобрения.

Вы можете проверить, что любой опубликованный псевдоним принадлежит нам:

```bash
npm info failproof
# Ищите: "ExosphereHost Inc." в поле maintainers
```

---

## Как работают псевдонимы

Каждый пакет-псевдоним:

1. Указывает `failproofai` как зависимость — так что настоящий пакет (включая его hook `postinstall`) запускается при установке
2. Предоставляет бинарный файл, соответствующий собственному имени (например `failprof-ai`), который передаёт все аргументы бинарному файлу `failproofai`

Прокси — это двухстрочный Node-скрипт; здесь нет логики, сетевых запросов и сбора данных сверх того, что делает сам `failproofai`.

---

## Если вы нашли имя, которое мы пропустили

Откройте issue в [failproofai/failproofai](https://github.com/failproofai/failproofai/issues), и мы его зарегистрируем.