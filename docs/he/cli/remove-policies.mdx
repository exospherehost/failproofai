---
---
title: הסרת מדיניות
description: "הסרת כניסות hook מ-settings של Claude Code"
---

```bash
failproofai policies --uninstall [policy-names...] [options]
```

הסרת כניסות failproofai hook מה-`settings.json` של Claude Code.

כינויים: `failproofai p -u`

## אפשרויות

| דגל | תיאור |
|------|-------------|
| `--scope user` | הסרה מהגדרות גלובליות (ברירת מחדל) |
| `--scope project` | הסרה מהגדרות פרויקט |
| `--scope local` | הסרה מהגדרות מקומיות |
| `--scope all` | הסרה מכל ההיקפים בבת אחת |
| `--custom` / `-c` | ניקוי של `customPoliciesPath` מתצורה |

## התנהגות

- **ללא שמות מדיניות** - הסרת כל כניסות failproofai hook מקובץ ההגדרות
- **שמות ספציפיים** - השבתת מדיניות אלה אך שמירה על hooks מותקנים

## דוגמאות

```bash
# הסרת כל hooks בעולם
failproofai policies --uninstall

# השבתת מדיניות ספציפית (שומרת hooks מותקנים)
failproofai policies --uninstall block-sudo

# הסרת hooks מכל היקף
failproofai policies --uninstall --scope all

# ניקוי נתיב מדיניות מותאמת
failproofai policies --uninstall --custom
```