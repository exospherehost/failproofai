---
title: Variabili d'ambiente
description: "Configura il comportamento di failproofai tramite variabili d'ambiente"
---

## Dashboard

| Variabile | Descrizione |
|----------|-------------|
| `PORT` | Porta della dashboard (predefinita: `8020`) |
| `CLAUDE_PROJECTS_PATH` | Sovrascrive il percorso in cui vengono cercate le cartelle dei progetti Claude Code |
| `FAILPROOFAI_DISABLE_PAGES=policies,projects` | Pagine della dashboard da nascondere, separate da virgola |
| `FAILPROOFAI_ALLOWED_DEV_ORIGINS` | Host/IP autorizzati ad accedere alle risorse di sviluppo. Equivalente a `--allowed-origins`. |

## Logging

| Variabile | Descrizione |
|----------|-------------|
| `FAILPROOFAI_LOG_LEVEL=info\|warn\|error` | Livello di log del server (predefinito: `warn`) |
| `FAILPROOFAI_HOOK_LOG_FILE` | Percorso personalizzato per il file di log degli hook, oppure `true` per il percorso predefinito (`~/.failproofai/logs/hooks.log`) |

## Telemetria

| Variabile | Descrizione |
|----------|-------------|
| `FAILPROOFAI_TELEMETRY_DISABLED=1` | Disabilita la telemetria anonima sull'utilizzo |

## LLM (per la valutazione delle policy)

| Variabile | Descrizione |
|----------|-------------|
| `FAILPROOFAI_LLM_BASE_URL` | Endpoint API dell'LLM (predefinito: `https://api.openai.com/v1`) |
| `FAILPROOFAI_LLM_API_KEY` | Chiave API per le policy basate su LLM |
| `FAILPROOFAI_LLM_MODEL` | Nome del modello (predefinito: `gpt-4o-mini`) |