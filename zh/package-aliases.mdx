---
title: 包别名
description: "已注册的防错别字别名及其工作原理"
icon: copy
---

## 官方包

npm 的权威包名为 **`failproofai`**：

```bash
npm install -g failproofai
# or
bun add -g failproofai
```

---

## 为什么我们持有这些别名

错别字抢注（Typosquatting）是一种常见的供应链攻击手段——恶意攻击者注册一个与热门包名仅差一个按键的包名，不知情的用户在拼错安装命令时，就会运行攻击者控制的代码，并获得完整的系统访问权限。这正是 Failproof AI 所致力于防御的威胁类型。

为了消除这一攻击面，**我们在 npm 上抢先注册了 `failproofai` 所有常见的拼写错误和格式变体**。这些包名均无法被第三方注册。每个别名都是一个轻量代理，安装后会委托给真正的 `failproofai` 包执行。

---

## 已注册的别名

**格式变体** —— "failproof ai" 的不同书写方式：

| 包名 | 状态 |
|---------|--------|
| `failproof` | ✅ 已发布 |
| `failproof-ai` | ⏳ 等待 npm 审核 |
| `fail-proof-ai` | ⏳ 等待 npm 审核 |
| `failproof_ai` | ⏳ 等待 npm 审核 |
| `fail_proof_ai` | ⏳ 等待 npm 审核 |
| `fail-proofai` | ⏳ 等待 npm 审核 |

**`failprof*` 错别字** —— "proof" 中缺少一个 `o`：

| 包名 | 状态 |
|---------|--------|
| `failprof` | ✅ 已发布 |
| `failprof-ai` | ✅ 已发布 |
| `failprofai` | ⏳ 等待 npm 审核 |
| `fail-prof-ai` | ⏳ 等待 npm 审核 |
| `failprof_ai` | ⏳ 等待 npm 审核 |

**`faliproof*` 错别字** —— `a` 和 `i` 互换位置：

| 包名 | 状态 |
|---------|--------|
| `faliproof` | ✅ 已发布 |
| `faliproof-ai` | ✅ 已发布 |
| `faliproofai` | ⏳ 等待 npm 审核 |

> **为什么显示"等待审核"？** npm 的防垃圾包政策会拦截那些在去除标点符号并进行相似度检测后与已有包名规范化结果相同的包名。我们已联系 npm 支持团队，申请以防抢注为由保留这些名称，待审批通过后即会激活。

您可以验证任何已发布的别名是否由我们持有：

```bash
npm info failproof
# Look for: "ExosphereHost Inc." in the maintainers field
```

---

## 别名的工作原理

每个别名包会：

1. 将 `failproofai` 列为依赖项——确保真正的包（包括其 `postinstall` 钩子设置）在安装时运行
2. 暴露一个与自身同名的可执行文件（例如 `failprof-ai`），将所有参数代理转发至 `failproofai` 二进制文件

代理逻辑是一个两行的 Node 脚本，不包含任何业务逻辑，不发起网络请求，也不收集除 `failproofai` 本身以外的任何数据。

---

## 如果您发现我们遗漏了某个名称

请在 [exospherehost/failproofai](https://github.com/exospherehost/failproofai/issues) 提交 Issue，我们会及时注册该名称。